# ข้อตกลงการทำงานใน Gum100UI

## บันทึกการเปลี่ยนแปลง — คำขอโดยตรงของผู้ใช้

- อ่าน `UI_CHANGELOG.md` ก่อนเริ่มแก้ เพื่อเข้าใจงานก่อนหน้าและข้อจำกัด
- หลังแก้/เพิ่ม/ปรับปรุงแต่ละชุดงานเสร็จ **ให้อัปเดต `UI_CHANGELOG.md` ทันที ก่อนเริ่มชุดงานใหม่หรือส่งข้อความว่างานเสร็จ** ไม่รอรวมบันทึกตอนท้ายหลายงาน
- ระบุวันที่ เหตุผล ไฟล์ selector/ฟังก์ชัน ก่อน–หลัง การทดสอบที่ทำจริง ข้อจำกัด และ cache version หากเปลี่ยน
- ถ้าเปลี่ยนพฤติกรรมจากบันทึกเดิม ให้ระบุว่ารายการใหม่แทนที่/ต่อยอดข้อใด เก็บประวัติเก่าไว้
- ไม่อ้างว่าทดสอบผ่านหรือ deploy แล้วหากไม่ได้ทำจริง แยกการทดสอบ preview/ข้อมูลจำลองออกจากระบบจริง
- เมื่อส่งมอบงาน แจ้งสั้น ๆ ว่าบันทึกแล้วและชี้ไปยังเอกสารนี้ ไม่ต้องขออนุญาตซ้ำเพื่อบันทึก

## ขอบเขต UI ปัจจุบัน

- คงธีมเดิมและความกระชับตามคำขอผู้ใช้ ใช้ `assets/app.css` เป็นหลัก
- รักษา id, class, data-* และความสัมพันธ์ DOM ที่ JavaScript ใช้; อ่านโค้ดส่วนที่เกี่ยวข้องก่อนปรับ
- อย่าทำให้ `[hidden]` โผล่เพราะกฎ display ใหม่ รักษาสถานะ active, read-only, disabled, warning และ reduced-motion
- เมื่อแก้ CSS/JS ให้อัปเดต query version ของไฟล์นั้นใน `index.html` และหน้าตัวอย่างที่เกี่ยวข้อง
- `preview/` เป็นหน้าตัวอย่างข้อมูลสมมติ ห้ามนำผลทดสอบ preview ไปอ้างเป็นผลทดสอบ backend จริง
- การแก้ในเครื่องไม่ใช่การเผยแพร่ ห้ามเขียนสถานะ push/deploy โดยไม่มีหลักฐาน
