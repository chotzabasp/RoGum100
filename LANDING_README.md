# หน้าแรก Gum100

เปิด `landing.html` เพื่อดูหน้าแรกใหม่ แยกจาก `index.html` ซึ่งยังเป็นระบบภายในเดิม

## เปิดดูในเครื่อง

จากโฟลเดอร์โปรเจกต์ ใช้ `node .claude/static-server.js` แล้วเปิด `http://localhost:8080/landing.html` หรือเปิดไฟล์ HTML โดยตรง การเข้าสู่ระบบควรทดสอบผ่าน HTTP และโดเมนที่อนุญาตใน Supabase

## ไฟล์สำหรับแก้ไข

- `landing.html`: ข้อความ โครงหน้า และปุ่มไปเครื่องมือเดิม
- `assets/landing.css`: สไตล์หน้าแรกโดยเฉพาะ
- `assets/landing.js`: รายละเอียดตัวละคร ปุ่มหยุดการเคลื่อนไหว การเลื่อนแผนที่มือถือ และแท็บตัวอย่าง
- `assets/landing/`: แผนที่และภาพมอนสเตอร์ทั้ง 9 ตัว พร้อม `SOURCES.md` บันทึกแหล่งที่มา
- `assets/app.js`: รองรับลิงก์ `index.html?auth=login` และ `index.html?auth=register` เพื่อเปิดแบบฟอร์มเดิมหลังตรวจเซสชัน ผู้ที่ล็อกอินแล้วเข้าแอปต่อได้เลย พารามิเตอร์ `auth` ที่ถูกต้องจะถูกนำออกหลังอ่านค่า โดยเก็บ query อื่นและ hash ไว้
- `index.html`: อัปเดตเวอร์ชันสคริปต์แอปสำหรับลิงก์เข้าสู่ระบบจากหน้าแรก

พิกัด `--x` และ `--y` ของตัวละครอิงจุดกึ่งกลางแมพในรูป 1280 × 1024 มอนสเตอร์ทุกตัวเป็นปุ่มที่รองรับเมาส์ การแตะ และคีย์บอร์ด กด Escape เพื่อปิดรายละเอียด บนมือถือเลื่อนแผนที่แนวนอนหรือใช้ปุ่มซ้าย/ขวาได้ ภาพเคลื่อนไหวหยุดได้และเคารพการตั้งค่า reduced motion

ข้อมูลในส่วนตัวอย่างเครื่องมือเป็นข้อมูลสมมติ ไม่เชื่อมบัญชีหรือฐานข้อมูล และไม่มีการดึงสถานะบอสจริง

## ก่อนเปลี่ยนหน้าเปิดเว็บจริง

รอบนี้ยังใช้ `landing.html` สำหรับตรวจแบบ ยังไม่สลับทางเข้าระบบหรืออัปโหลดขึ้น GitHub

หากจะเปลี่ยนหน้าเริ่มต้นเป็นหน้าใหม่นี้ ต้องย้ายแอปเดิมจาก `index.html` เป็น `app.html` จากนั้นเปลี่ยน `landing.html` เป็น `index.html` และแก้ลิงก์เข้าแอปทั้งหมด พร้อมดูแล:

1. ลิงก์เก่า `/#accounting`, `/#home`, `/#items`, `/#farm`, `/#timers`, `/#pricing`, `/#settings`, `/#admin` ต้องส่งไปแอปโดยรักษา query/hash ที่เกี่ยวข้อง
2. Supabase callback ยืนยันอีเมลและกู้รหัสผ่าน รวมถึง redirect allowlist ของบริการจริง
3. ทางเข้ารุ่นเก่า `mvp-watch.html` และ tests ที่อ้างอิง `index.html`
4. metadata canonical และภาพแชร์ของหน้าแรก
5. ตรวจสิทธิ์ใช้ภาพจากแหล่งอ้างอิงก่อนเผยแพร่ ตามบันทึกใน `assets/landing/SOURCES.md`

อย่าโหลด `assets/app.js` ในหน้าแรก เพราะต้องมี DOM ของระบบภายในครบ

## ผลตรวจตัวอย่างในเครื่อง

- เปิดหน้าแรกและกดเข้าแอปเดิมได้ที่ `index.html#accounting`
- ตรวจความกว้าง 320, 375, 820 และ 1440px ไม่พบหน้าเว็บล้นแนวนอน แผนที่มือถือเลื่อนแยกภายในพื้นที่ของแผนที่
- เปิดรายละเอียดตัวละครครบ 9 จุด ปิดด้วย Escape ได้ และหยุดภาพเคลื่อนไหวได้
- สลับตัวอย่างเครื่องมือครบ 4 หมวด ใช้ลูกศรบนคีย์บอร์ดสลับแท็บได้ และไม่ดัก Ctrl+Home ของเบราว์เซอร์
- ตรวจไฟล์ภาพและลิงก์ในเครื่อง ไม่มีไฟล์อ้างอิงหาย ไม่มี ID ซ้ำ และไม่มี anchor ที่ขาดปลายทาง
- JavaScript ผ่าน `node --check` รูปแบบ patch ผ่าน `git diff --check`
- ไม่ได้ทดสอบเข้าสู่บัญชีจริง ชำระเงิน หรือเปลี่ยนข้อมูลในระบบ

พรีวิวของรอบนี้เปิดที่ `http://127.0.0.1:8765/landing.html` ระหว่างที่ local server ยังทำงาน
