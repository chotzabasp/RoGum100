# หน้าแรก Gum100

หน้าแรกของเว็บคือ `index.html` (gum100.com) ระบบเครื่องมือเดิมย้ายไปอยู่ที่ `app.html` (สลับเมื่อ 2026-09-26)

## เปิดดูในเครื่อง

จากโฟลเดอร์โปรเจกต์ ใช้ `node .claude/static-server.js` แล้วเปิด `http://localhost:8080/` (หน้าแรก) หรือ `http://localhost:8080/app.html` (เครื่องมือ) หรือเปิดไฟล์ HTML โดยตรง การเข้าสู่ระบบควรทดสอบผ่าน HTTP และโดเมนที่อนุญาตใน Supabase

## ไฟล์สำหรับแก้ไข

- `index.html`: ข้อความ โครงหน้า และปุ่มไปเครื่องมือเดิม
- `assets/landing.css`: สไตล์หน้าแรกโดยเฉพาะ
- `assets/landing.js`: รายละเอียดตัวละคร ปุ่มหยุดการเคลื่อนไหว การเลื่อนแผนที่มือถือ และแท็บตัวอย่าง
- `assets/landing/`: แผนที่และภาพมอนสเตอร์ทั้ง 9 ตัว พร้อม `SOURCES.md` บันทึกแหล่งที่มา
- `assets/app.js`: รองรับลิงก์ `app.html?auth=login` และ `app.html?auth=register` เพื่อเปิดแบบฟอร์มเดิมหลังตรวจเซสชัน ผู้ที่ล็อกอินแล้วเข้าแอปต่อได้เลย พารามิเตอร์ `auth` ที่ถูกต้องจะถูกนำออกหลังอ่านค่า โดยเก็บ query อื่นและ hash ไว้
- `index.html` (หน้าแรก): สคริปต์ต้นหน้าส่งต่อไป `app.html` ทันทีเมื่อเป็นลิงก์เก่า `/#หน้า` (accounting/home/farm/timers/items/pricing/settings/admin), มี `?auth=` / พารามิเตอร์ล็อกอินของ Supabase หรือมีการล็อกอินค้างไว้ในเครื่อง
- `app.html`: ระบบเครื่องมือเดิม (เดิมชื่อ index.html) · `mvp-watch.html` และปุ่มใน `admin.html` ชี้มาที่นี่

พิกัด `--x` และ `--y` ของตัวละครอิงจุดกึ่งกลางแมพในรูป 1280 × 1024 มอนสเตอร์ทุกตัวเป็นปุ่มที่รองรับเมาส์ การแตะ และคีย์บอร์ด กด Escape เพื่อปิดรายละเอียด บนมือถือเลื่อนแผนที่แนวนอนหรือใช้ปุ่มซ้าย/ขวาได้ ภาพเคลื่อนไหวหยุดได้และเคารพการตั้งค่า reduced motion

ข้อมูลในส่วนตัวอย่างเครื่องมือเป็นข้อมูลสมมติ ไม่เชื่อมบัญชีหรือฐานข้อมูล และไม่มีการดึงสถานะบอสจริง

## หมายเหตุ

อย่าโหลด `assets/app.js` ในหน้าแรก เพราะต้องมี DOM ของระบบภายในครบ

## ผลตรวจตัวอย่างในเครื่อง

- เปิดหน้าแรกและกดเข้าแอปเดิมได้ที่ `app.html#accounting`
- ตรวจความกว้าง 320, 375, 820 และ 1440px ไม่พบหน้าเว็บล้นแนวนอน แผนที่มือถือเลื่อนแยกภายในพื้นที่ของแผนที่
- เปิดรายละเอียดตัวละครครบ 9 จุด ปิดด้วย Escape ได้ และหยุดภาพเคลื่อนไหวได้
- สลับตัวอย่างเครื่องมือครบ 4 หมวด ใช้ลูกศรบนคีย์บอร์ดสลับแท็บได้ และไม่ดัก Ctrl+Home ของเบราว์เซอร์
- ตรวจไฟล์ภาพและลิงก์ในเครื่อง ไม่มีไฟล์อ้างอิงหาย ไม่มี ID ซ้ำ และไม่มี anchor ที่ขาดปลายทาง
- JavaScript ผ่าน `node --check` รูปแบบ patch ผ่าน `git diff --check`
- ไม่ได้ทดสอบเข้าสู่บัญชีจริง ชำระเงิน หรือเปลี่ยนข้อมูลในระบบ

