-- ============================================================
-- ล้างข้อมูลหน้า "จับเวลาบอส" ทั้งหมดของทุกบัญชีในระบบ (ไม่ใช่แค่บัญชีทดสอบ)
-- เตรียมพื้นที่ว่างสำหรับระบบใหม่ที่ไม่แยกตามเซิร์ฟเวอร์อีกต่อไป (รายการบอสรวมเป็นชุดเดียวต่อบัญชี/ปาร์ตี้)
--
-- คำเตือน: ลบแล้วกู้คืนไม่ได้ ถ้าไม่ได้เปิด Point-in-time recovery/Pro backup ไว้บน Supabase
-- ข้อมูลจะหายถาวร — ตรวจสอบให้แน่ใจก่อนรัน
--
-- ไม่แตะ: bosses (แคตตาล็อกกลาง), custom_bosses (นิยามบอสที่ผู้ใช้สร้างเอง — ชื่อ/รูป/เวลาเกิด/ไอเทม)
-- ============================================================
begin;

delete from public.kill_items;
delete from public.kills;
delete from public.user_bosses;
delete from public.custom_boss_timers;

commit;
